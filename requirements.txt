# API server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Request/response models
pydantic>=2.0

# Azure OpenAI — reasoning + function calling (API key auth)
openai>=1.0.0

# Azure Speech — REST (batch STT) + SDK (streaming STT push stream)
requests>=2.31.0
azure-cognitiveservices-speech>=1.34.0

# PostgreSQL + pgvector — context search
psycopg2-binary>=2.9.0

# MongoDB — orders
pymongo>=4.6.0

# Load .env for local development
python-dotenv>=1.0.0

# WebSocket support
websockets>=12.0
